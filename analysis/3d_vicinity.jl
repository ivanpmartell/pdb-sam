#TODO
elseif args["type"] == "2d"
    #same as summarize clusters
elseif args["type"] == "3d"
    #.cif (only chain) or .mmcif (ss structure added) file 
elseif args["type"] == "contact"
    #get contactmap from biostructures c-betas with files above
end